#!/bin/bash

if [[ "$1" == "-h" ]] ; then
    echo "$0 [-4]"
    exit
fi

EXTRAOPTS=
if [[ "$1" == "-4" ]] ; then
    EXTRAOPTS="-o Acquire::ForceIPv4=true"
fi

sudo apt-get ${EXTRAOPTS} update && sudo apt-get ${EXTRAOPTS} upgrade && sudo apt-get autoremove
